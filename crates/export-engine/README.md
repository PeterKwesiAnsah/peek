# export-engine

Export and serialization engine for the [Peek](https://github.com/ankittk/peek) process intelligence tool.

Renders process snapshots to JSON, Markdown, HTML, and PDF. Used by the peek CLI `--export` and `--json` / `--json-snapshot` options.

Part of the [peek](https://github.com/ankittk/peek) workspace.
